import React from 'react';

import './style.css';

const WithCenteredText = ({ children }) => (
  <div className="withCenteredText--root">
    {children}
  </div>
);

export default WithCenteredText;
