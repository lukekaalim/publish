export const LANDING_MODES = ['basic', 'countdown'];

const INITAL_STATE = {
  landingMode: 0,
};

const site = (state = INITAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default site;
